people = ["alice", "bob", "charlie", "mr. karabalabaloofal"]

// $..$ evals at runtime by calling an LLM and assings result to the var
// the type annotation actually is ensured by the language runtime

longest_name: string = $who has the longest name in {people}$

assert longest_name == people[3] // yup true
print(longest_name)
